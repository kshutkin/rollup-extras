{
  "private": true,
  "scripts": {
    "build": "pnpm --aggregate-output --stream -r build",
    "test": "pnpm --aggregate-output --stream -r test",
    "lint": "pnpm --aggregate-output --stream -r lint",
    "release": "pnpm -r semantic-release"
  }
}
