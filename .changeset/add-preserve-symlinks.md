---
"@rollup-extras/plugin-copy": minor
---

Added `preserveSymlinks` option to recreate symlinks as symlinks at the destination instead of dereferencing them. Only effective with `emitFiles: false`.
